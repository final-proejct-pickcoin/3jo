<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.finalproject.pickcoin.repository.AssetRepository">

<!-- 전체 코인정보 검색 -->
<select id="get_asset_list" resultType="com.finalproject.pickcoin.domain.Asset">
	SELECT * FROM asset
	ORDER BY  asset_id
</select>

<!-- 코인 정보 -->	
<select id="get_asset_info" parameterType="Integer" resultType="com.finalproject.pickcoin.domain.Asset">
	SELECT * FROM asset
	WHERE asset_id = #{asset_id}
</select>
	

</mapper>